// Instagram App ID used for the native Reels sharing pasteboard handoff.
// This must match the Facebook App ID configured in your Meta Developer Console
// and the URL scheme registered in app.json / Info.plist.
export const INSTAGRAM_APP_ID = process.env.EXPO_PUBLIC_INSTAGRAM_APP_ID ?? '';
